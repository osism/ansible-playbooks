---
- name: Apply role atlantis
  hosts: "{{ hosts_atlantis|default('atlantis') }}"
  serial: "{{ osism_serial['atlantis']|default(osism_serial_default)|default(0) }}"

  collections:
    - osism.services

  roles:
    - role: atlantis
