---
- name: Apply role osquery
  hosts: all
  serial: "{{ osism_serial['osquery']|default('0') }}"
  strategy: "{{ osism_strategy|default('linear') }}"

  collections:
    - osism.services

  roles:
    - role: osquery
