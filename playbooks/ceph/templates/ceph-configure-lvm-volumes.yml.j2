---
#
# This is Ceph LVM configuration for {{ inventory_hostname }}
# generated by ceph-configure-lvm-volumes playbook.
#
{{
  _ceph_configure_lvm_config_data |
  ansible.builtin.to_nice_yaml(indent=2)
}}
