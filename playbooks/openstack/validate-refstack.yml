---
- name: Run refstack validator
  hosts: "{{ hosts_refstack|default('manager') }}"
  strategy: "{{ osism_strategy|default('linear') }}"

  collections:
    - osism.validations

  roles:
    - role: refstack
