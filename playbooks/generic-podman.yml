---
- name: Apply role podman
  hosts: podman
  serial: "{{ osism_serial['podman']|default('1') }}"
  strategy: "{{ osism_strategy|default('linear') }}"

  collections:
    - osism.commons

  roles:
    - role: podman
