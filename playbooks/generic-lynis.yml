---
- name: Apply role lynis
  hosts: all
  serial: "{{ osism_serial['lynis']|default('0') }}"
  strategy: "{{ osism_strategy|default('linear') }}"

  collections:
    - osism.commons

  roles:
    - role: lynis
