---
- name: Apply role tailscale
  hosts: "{{ hosts_tailscale|default('tailscale') }}"
  serial: "{{ osism_serial['tailscale']|default('0') }}"

  collections:
    - osism.services

  roles:
    - role: tailscale
