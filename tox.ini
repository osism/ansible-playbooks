[tox]
minversion = 1.8
skipsdist = true

[testenv]
basepython=python2.7
passenv = *
deps =
    -rtest-requirements.txt
whitelist_externals =
    bash

[testenv:lint]
commands =
    bash -c "ansible-lint playbooks/*"
    yamllint -c .yamllint.yml playbooks tasks
